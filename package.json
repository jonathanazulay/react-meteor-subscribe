{
  "name": "meteor-redux",
  "version": "0.0.1",
  "description": "A small, simple library for handling data and subscriptions when using Redux + Meteor",
  "main": "./lib/index.js",
  "scripts": {
    "build": "babel src -d lib",
    "clean": "rm -rf ./lib",
    "prepublish": "npm run clean && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/jonathanazulay/meteor-redux.git"
  },
  "files": [
    "lib", "src"
  ],
  "author": "Jonathan Azulay <jonathanazulay@me.com>",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.9.0",
    "babel-preset-es2015": "^6.9.0",
    "babel-preset-react": "^6.5.0"
  },
  "peerDependencies": {
    "react": "^0.14.0 || ^15.0.0-0"
  }
}
